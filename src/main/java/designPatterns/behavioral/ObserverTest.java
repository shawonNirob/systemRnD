package designPatterns.behavioral;

import java.util.ArrayList;
import java.util.List;

/*
public class ObserverTest {
    public static void main(String[] args) {
        ObserverTest weatherStation = new ObserverTest();

        // Create displays and register them with the weather station
        CurrentConditionsDisplay currentDisplay = new CurrentConditionsDisplay(weatherStation);
        ForecastDisplay forecastDisplay = new ForecastDisplay(weatherStation);

        // Simulate new weather measurements
        weatherStation.setMeasurements(26.5f, 65.0f, 1012.0f);
        weatherStation.setMeasurements(27.0f, 70.0f, 998.0f);
        weatherStation.setMeasurements(25.0f, 90.0f, 1005.0f);
    }
}


 */